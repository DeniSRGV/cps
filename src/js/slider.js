const slider = ()=> {



          const swiperRepair = new Swiper('.swiper', {
            // Optional parameters
                loop: true,
                spaceBetween: 16,
                slidesPerView: 'auto',
                pagination: {
                el: '.swiper-pagination',
                }
        });
};

export default slider;
